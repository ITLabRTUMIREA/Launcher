<Window x:Class="Launcher.WPF.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Launcher.WPF.Windows"
        xmlns:viewmodels="clr-namespace:Launcher.WPF.ViewModels"
        mc:Ignorable="d"
        Title="LoginWindow" Height="477" Width="245">
    <Window.DataContext>
        <viewmodels:LoginVM/>
    </Window.DataContext>
    <Grid>
        <Button HorizontalAlignment="Center" VerticalAlignment="Center" Content="Войти" Command="{Binding LoginCommand}"/>
        <WebBrowser Visibility="{Binding BrowserVisibility}"/>
    </Grid>
</Window>
